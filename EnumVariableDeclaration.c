#include<stdio.h> // it is also requered becuse we called all function for this library
void main() // the main function the system start from compiling for that function  that function
{
	enum day { sunday, monday, tuesday, wendesday, tharsday, friday , satarday };
	// hare declare enum identifier name
	// enum identifier {Values};
	//enum identifier variable;
	// variable = values in barace
	enum day week_start, week_end;
	// hare we declare the enum variable
	week_start = sunday;     // hare assignment of enums identifier values
	week_end = satarday;  // hare assignment of enums identifier values


  // that program have no out put becuse ve just declare variable 
  // but dosent do anything means Operations
}
