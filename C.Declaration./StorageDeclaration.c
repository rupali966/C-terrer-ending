#include<stdio.h> // it is also requered becuse we called all function for this library
void main() // the main function the system start from compiling for that function  that function
{
	auto float x , y ;
	// hare declare the first storage-class then data-type  and than variable
	static  int code;
	// starageclass datatype variable;
	register short int count;
	extern double deveation;
	auto char c;
}
  // that program have no out put becuse ve just declare variable 
  // but dosent do anything means Operations
